import styled from "styled-components";

const FooterWrapper = styled.footer`
  ul {
    display: flex;
    flex-direction: row;
  }
  li {
    list-style: none;
    padding: 0 5px;
  }
`;

export default FooterWrapper;
